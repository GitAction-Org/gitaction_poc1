name: My Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      MY_VARIABLE: "Hello,I am ${{var.USER_NAME}} using GitHub Actions!"

    steps:
    
    - name: Print Variable
      run: echo $MY_VARIABLE
